# Mnemosis 存储功能介绍（技术人员版）

> 这份文档写给想接入 Mnemosis 的开发者、部署者和二次开发者。
> 默认你懂 SQL / 数据库的基本概念，但每个技术结论都会补一句大白话“为什么这么做”。
> 目标是：看完能回答“数据放哪、怎么存取、快不快、稳不稳、能不能备份迁移”。

---

## 1. 一句话总结

Mnemosis 的存储层 = **一个 SQLite 单文件数据库 + 三层内存缓存 + 一批为了“快”而做的 SQL / 索引优化**。

默认零外部依赖：不需要 PostgreSQL、不需要 Redis、不需要向量数据库。
整个记忆库就是一个 `.db` 文件，拷走它，记忆就带走了。

> 大白话：这不是“为了炫技”的架构，而是单机 / 单 Agent 场景下最省心、最不容易坏的方案。

---

## 2. 为什么选 SQLite

| 优点 | 说明 |
|---|---|
| 单文件、零运维 | 一个 Agent 或一台 NAS 就是典型场景，不需要装数据库服务 |
| 事务与 ACID | 写入要么全部成功、要么全部回滚，记忆不会写到一半坏掉 |
| 性能足够 | 10 万条级记忆库，热查询约 1~2ms；高频缓存命中约 0.1~0.3ms |
| 可移植 | 文件即备份；也支持 `":memory:"` 纯内存模式（测试 / 临时用） |
| 远程部署简单 | 库文件放 VPS / NAS，通过 MCP 服务对外提供，不必把数据库暴露成公网服务 |

> 大白话：单机场景给记忆单独装一个数据库服务属于“杀鸡用牛刀”。SQLite 正好卡在“够快、够稳、不用装”的点上。

什么时候不适合：**多进程同时高频写同一个文件**。SQLite 是单写者数据库，多个 Agent 同时写建议串行化或由 MCP 服务统一转发；读并发没有问题。

> 警告：不要把 `memory.db` 直接放在 NFS / SMB 这类网络共享盘上让多台机器共用——SQLite 的文件锁在网络文件系统上不可靠，可能导致数据库损坏。远程部署的正确姿势是：库留在服务器本地，对外只开 MCP 服务接口，而不是共享数据库文件本身。

---

## 3. 数据模型：5 张表

全部是普通 SQLite 表：

### `memories` —— 记忆主表

| 字段 | 作用 |
|---|---|
| `id` (TEXT PRIMARY KEY) | 记忆唯一 ID（UUID） |
| `kind` | `episodic`（情景）/ `semantic`（语义） |
| `content` / `content_hash` | 内容原文 + 哈希（语义去重用） |
| `source_json` / `cues_json` | 来源信息与线索的 JSON 快照 |
| `created_at` / `updated_at` / `last_access_at` | 时间戳 |
| `importance` / `strength` / `confidence` | 重要度 / 强度 / 置信度 |
| `storage_strength` | 长期存储强度（涨得慢、掉得慢） |
| `retrieval_successes` / `retrieval_failures` | 回忆成功 / 失败次数 |
| `review_streak` / `last_review_at` | 复习状态 |
| `revision_count` | 被修改过几次（再巩固） |
| `status` | `active` / `recycled`（回收站） |
| `seq` | 全局自增序号（按时间排序用） |
| `context` / `affect` / `evidence_count` | 情境 / 情绪 / 证据数 |

`source_json` / `cues_json` 等 JSON 字段仅作**存储和透传**，不参与任何索引与
SQL 过滤（线索另有 `cues` 表做正式索引），所以不存在 SQLite JSON 函数性能瓶颈。

> 大白话：JSON 字段只是“打包带走的附件”，数据库不会去拆解和检索它们，所以不会因为解析 JSON 把速度拖慢。

**关于 UUID 主键**：`id` 用随机 UUID（TEXT），好处是分布式环境下无需中心发号、
不会冲突；代价是随机插入会让 B-tree 页分裂多一点。实测 10 万 / 100 万级构建
（约 27s / 6 分钟）说明写入放大在可接受范围；库文件变大后可 `VACUUM` 整理。

### `links` —— 关联图

`(src, dst, weight)`，主键 `(src, dst)`。
记录“记忆 A 和记忆 B 有关联，权重多少”，用于关联激活、模式补全等。
应用层保证只写**规范方向**（`src < dst`），同一对记忆不会出现 `(A,B)` 和 `(B,A)` 两行；
旧库首次打开会自动迁移去重。

### `cues` —— 线索索引

`(cue, memory_id)`，主键 `(cue, memory_id)`。
线索就是“想起这条记忆的钥匙”，一张记忆可以有多把钥匙。

### `terms` —— 分词索引

`(term, memory_id, kind)`，主键 `(term, memory_id)`。
记忆内容分词后的词条，检索时用来快速找候选。

### `settings` —— 键值设置

存 `decay_rate`（遗忘曲线参数）等运行期配置，重启后自动恢复。

> 大白话：`memories` 是“卡片本身”，`links` 是“卡片之间的线”，`cues` / `terms` 是“查卡片的目录”，`settings` 是“设置页”。

**Schema 迁移**：内置自愈式迁移——每次打开数据库时自动检测并补齐缺失的列和索引，
旧库首次打开会自动加列、重建缺失索引、把 links 迁移成规范方向；升级版本不需要手动跑 SQL。

---

## 4. 索引设计：为什么快

| 索引 | 服务谁 | 说明 |
|---|---|---|
| `sqlite_autoindex_memories_1`（id 主键） | 按 ID 批量取记忆 | TEXT 主键自动索引 |
| `idx_semantic_hash`（部分唯一索引） | 语义去重 | `(kind, content_hash) WHERE kind='semantic'`，只约束语义记忆 |
| `idx_memories_status_seq` | 最近记忆 | `(status, seq)` |
| `idx_memories_status_importance` | 最强记忆 | `(status, importance)` |
| `idx_memories_status_kind` | 按类型过滤 | `(status, kind)` |
| `cues` 主键 | 线索反查 | 覆盖索引（只要 memory_id，不用回表） |
| `terms` 主键 | 词条反查 | 词条→ID 快速定位；带 kind 过滤时仍需读表拿 kind，属半覆盖 |
| `idx_links_dst` | 反向关联 | 图遍历用 |

**为什么 `links` 要有 `idx_links_dst`**：因为边只存规范方向（`src < dst`），
查某个节点的所有关联时要查两次——`src=节点`（走主键）和 `dst=节点`（走这个索引）。

**为什么不用 SQLite 自带的 FTS5 全文索引**：FTS5 是“黑盒”索引，分词和打分规则
固定，难以和遗忘曲线、重要度、线索做深度联合打分；自建 `terms` 表能完全控制
索引结构和打分逻辑，10 万级实测也足够快。

**重点：CI 里有 SQL 执行计划巡检（`audit_query_plans`）**，每次提交都检查核心查询是否仍走预期索引。
历史上修过一个真实事故：`get_many` 因带 `status` 过滤被 SQLite 优化器改成状态索引全扫，50 次查询从 1ms 变成 1434ms——现在这类退化会被 CI 直接拦下。

> 大白话：CI 就像个严厉的教导主任，每次代码提交都要检查 SQLite 有没有偷懒走全表扫描，发现偷懒直接打回重做。

> 大白话：索引像书的目录。慢往往不是数据多，而是 SQLite 没走目录、把整本书翻了一遍。CI 就是盯着它“必须翻目录”。“覆盖索引”更省：目录页里直接写了答案，连翻到正文（回表）那一步都省了。

---

## 5. 写路径：一条记忆是怎么落库的

`remember(...)` 大致做四件事：

1. **打分与建模**：算重要度、贴时间戳、建 `MemoryItem`。
2. **写入主表**：
   - 情景记忆：直接 INSERT；
   - 语义记忆：按 `(kind, content_hash)` 去重，存在就合并证据数。
3. **写目录**：`cues`、`terms` 批量写入。
4. **建关联**：和“共享线索 / 同现”的记忆建 `links`（带权重）。

### 批量写入：`remember_many` / `remember_many_chunked`

`remember_many` 把上面这些全部批量化：

- 一个事务内 `executemany` 插入；
- `seq` 用 `INSERT ... SELECT MAX(seq)+1` 分配；批量路径先 `BEGIN IMMEDIATE`
  抢到写锁再读 `MAX(seq)`，多进程并发写也不会重号（依赖 SQLite 单写者串行化；
  注意：机制上不重号，但业务层仍不推荐多进程写同一文件，详见第 9 节的锁超时风险）；
  单条 `remember` 由单个 `INSERT ... SELECT` 完成，语句本身原子；如果另一进程的
  快照已过期，SQLite 会报 `database is locked`（SQLITE_BUSY 类错误）而不是静默重号，
  业务层捕获重试即可；
- 关联边数按规模自动收缩（起步 32 条 / 记忆，10 万条时精确为 10 条，
  12.5 万条及以上固定为 8 条（下限兜底）），
  避免边表把库写爆。注意：这个预算是**每条记忆主动发起建链的次数上限**；
  一条无向边由某一端主动发起即可，不需要两端各发一次，所以边数上限 ≈
  记忆数 × 预算（10 万 × 10 = 100 万），实测约 98 万条，接近上限。
  握手定理（总度数 = 2 × 边数）仍然成立：每条新边让两端度数各 +1，
  平均度数 ≈ 2 × 98 万 ÷ 10 万 ≈ 20。旧双向模型里 1 条逻辑边存 2 行
  （A→B 和 B→A），所以行数（196 万）是逻辑边数（98 万）的两倍；
  现在只存一行规范方向，行数（98 万）就等于逻辑边数；
- 关联边按**无向边**处理，每条边只存一行规范方向（ID 小的在前），检索时两个方向共用；
  早期版本曾存双向两行（10 万条约 196 万行），近期优化后减半为约 98 万行，检索结果不变。

`remember_many_chunked` 是 10 万条级导入专用路径：分块写入 + 增量建链 + 批量导入模式。

> 大白话：一口吃不成胖子，大导入切成小块喂，边吃边建关联，免得把内存撑爆。

> 注意：`remember_many_chunked` **不是原子操作**——它按块提交，如果中途失败，前面已导入的块会留在库里。大导入建议用可重试的流程（先清空目标再重导）。

> 实测（10 万条）：
> - `remember_many_chunked` 约 **25~27 秒**（旧路径约 6~7 分钟，最初版本要 2 小时以上）；
> - 构建期峰值 RSS 约 **620MB**，数据库文件约 **543MB**；100 万条约 6 分钟 / 约 5.1GB（峰值内存约 2GB）。

> 大白话：10 万条记忆各主动找 10 个朋友，总共发起 100 万次交友。逻辑上 A 找 B 和
> B 找 A 是同一条边，库里只存一行（规范方向）。虽然偶尔会出现“A 找 B 且 B 也找 A”
> 的双向尝试，但入库时通过唯一索引自动去重，所以实际只少了约 2 万条，实测 98 万行。
> 早期版本 A 找 B 存一行、B 找 A 又存一行，所以行数翻倍变成 196 万。
> 平均每个记忆的关联数约 20，是因为 A 主动连了 B 后，B 的关联数也会 +1。

---

## 6. 读路径：一次检索发生了什么

`recall(query)` 的链路：

```text
分词 / 同义词扩展
  → 查词频（df，决定哪些词值得当候选）
  → 取候选 ID（走 terms/cues 索引）
  → 打分（关键词重叠 + 重要度 + 遗忘曲线 + 情境/情绪/置信度…）
  → 可选：向量重排（只对 top-64 候选）
  → 图后处理（关联激活 / 模式补全 / 模式分离 / 竞争抑制）
  → 强化命中项、记录失败、安排复习
```

几个“为了快”的关键实现：

- **高频词跳过**：单词语频（df）太大时不再物化全部候选 ID（候选上限 5000），直接进兜底池，避免几万条候选。
- **零命中兜底**：无词法信号时用“最近 150 + 最强 50”双池，走 `(status, seq)` / `(status, importance)` 索引，查询只要几毫秒。
- **批量取 ID**：`get_many` 小列表用 `VALUES` CTE，大列表（≥64）用 `CROSS JOIN json_each`，状态过滤下推到 SQL——保证任何规模都走主键索引。

> 大白话：查少量 ID 用常规套路，查大量 ID 就换一个 SQLite 更喜欢的姿势，确保它乖乖走索引。
- **后处理限流**：泛查询跳过竞争抑制 / 激活扩展，单次召回最多抑制 12 条。

> 大白话：检索不是“把整库读出来再比”，而是“先用目录圈出一小撮候选，再对候选精算分数”。

---

## 7. 内存缓存：三层加速器 + 睡眠后预热

| 缓存 | 存什么 | 上限 / 策略 | 失效 |
|---|---|---|---|
| 词频 / 倒排缓存 | 词条 → ID、词频 | 词频和倒排**各** 5 万条上限（FIFO） | 写操作全量清空 |
| 嵌入向量缓存 | 文本 → 向量 | 默认约 512MB（按真实内存估算），FIFO | 写操作全量清空；达到 512MB 上限时按字节/FIFO 淘汰 |
| 兜底结果缓存 | 泛查询结果（id + 分数 + 原因） | 默认 32 条、TTL 15 秒、驱逐率高时自动扩容到 256 | remember / update / sleep / review 等写路径全量清空 |

> 目前统一采用**全量清空**策略（简单可靠，没有细粒度局部失效）：任何写路径都会让相关缓存作废，保证不会读到旧数据。
> 代价是高频写入场景下缓存命中率会下降；写入密集的业务可调大兜底缓存 TTL / 上限（引擎构造参数）来缓解。

> 为什么用 FIFO 不用 LRU？词频分布是典型的幂律（少数词极热、绝大多数词低频），
> FIFO 实现最简单、内存开销几乎为零；新词往往比老词更有检索价值，
> 牺牲极端热点的少量命中率换取零开销，是划算的。

缓存命中后仍会**重新取最新记忆内容**再返回，不会把旧文本喂给 Agent；写入即失效，缓存不会让你读到“刚删掉”的记忆。

> 大白话：缓存只帮你记“去哪找”，不帮你记“找到的内容”，保证你拿到的永远是最新鲜的。

### 睡眠后预热（post-sleep warmup）

`sleep()` 会做大量数据库整理，可能把操作系统页缓存和 SQLite 缓冲“挤出去”，导致睡醒后第一次查询变慢。所以睡眠结束后会启动一个后台线程，按以下规则挑出最多 **8 个线索（cue）** 预先查一遍，把缓存和页缓存“暖起来”：

```
预热得分 = (0.6 × 重要度 + 0.4 × (1 - 可提取度)) + 0.2 × 热度占比
没有热度信号时：= 0.6 × 重要度 + 0.4 × (1 - 可提取度)  ← 与旧版完全一致
```

- 重要度高的记忆优先；
- 快要遗忘的（可提取度低）优先，相当于“趁还记得先巩固”；
- 最近常被问到的线索优先（热度维度，来自最近 100 条查询日志）。
热度作为“加分项”，没有热度的记忆排序与旧版完全一致；只有最近被问过的线索会额外加分。

`stats()["warmup"]` 会暴露 `cues` / `recalls` / `hits` / `hit_rate`，可以观察预热命中率：

```python
engine.sleep()
print(engine.stats()["warmup"])
# {'cues': 8, 'recalls': 6, 'hits': 4, 'hit_rate': 0.6667}
```

`is_active` 表示“预热观测窗口（睡眠后 5 分钟内）是否仍有效”。窗口过期后，
`recalls` / `hits` / `hit_rate` 保留的是上一轮的最终数值，配合 `is_active=False`
一起看，避免把历史数据当成实时数据。

> 大白话：睡眠像“整理房间”，整理完桌子会乱；预热就是“提前把常用东西摆回手边”，让用户醒来第一句就有响应速度。

---

## 8. 可选：向量副文件（不开 embedding 就没有）

默认只用一个 `memory.db`。开启 embedding（Ollama 或 OpenAI 兼容接口）后，主库旁边会多出两个 SQLite 副文件：

| 文件 | 存什么 | 丢了会怎样 |
|---|---|---|
| `memory.db.cache` | 文本哈希 → 向量 的持久缓存（避免反复调 embedding API） | 只是变慢：重新调 API 计算即可，记忆无损 |
| `memory.db.vec` | LSH 向量索引（向量 + 桶），供语义重排 / 语义召回 | 语义检索失效；跑 `rebuild_missing_vectors()`（或 MCP 工具 `rebuild_vectors`）可重建 |

- 副文件都是可再生的：删掉它们，关键词 / n-gram 检索完全不受影响；
- 备份时最好把主库和两个副文件一起拷走；如果只拷主库，记得先跑一次重建向量；
- `.cache` 只存 SHA-256 哈希和向量，不存原文；`.vec` 按记忆 id 关联，不存内容。
- 运行期内存：`.vec` 不是把全部向量常驻内存——10k 条以内会缓存一个全量矩阵
  （几 MB~几十 MB，取决于向量维度）；超过 10k 只按查询加载桶内候选。库的 SQLite
  页缓存另算，小内存机器按“几十 MB 到几百 MB”的量级预留即可。
  100k 热查询 p99 ~1.8ms 的前提是 OS 页缓存已就绪（LSH 桶和向量页都被缓存了），
  不是每次查询都产生物理磁盘 IO。100k 规模下 LSH 索引自身的应用层内存只有
  几十 MB 级，大头在 OS 页缓存，可用 `PRAGMA cache_size` 控制上限。
- **向量约束**：距离度量固定为**余弦相似度**；维度没有硬编码上限，但**同一个索引内
  维度必须一致**——混用不同维度会出错。换 Embedding 模型（维度变化）时请重建 `.vec`
  索引（删掉后跑 `rebuild_missing_vectors()`）。
- 主库记忆被删除 / 修改时，两个副文件现在**会同步**：`update` 改了内容会重新嵌入并刷新向量；`forget` 移入回收站会删除向量，`restore` 恢复时会自动补回；`purge` 硬删除和 `sleep` 整理后都会清理回收站里的向量（`sleep` 单次最多清 1 万条，剩余的留给后续睡眠）。

> 大白话：主库和副文件是“主从关系”，主库删了，副文件也会跟着大扫除，保持步调一致。

> 大白话：主库是“记忆本身”，两个副文件只是“语义检索的加速器”。加速器坏了可以重建，记忆本身不会丢。

---

## 9. 并发与可靠性

- **线程安全**：底层连接用 `check_same_thread=False` 允许跨线程共享；
  后端所有方法用全局 `RLock` 包裹（单连接 + 全局锁模型），4 线程并发写读冒烟 0 错误。
  读写并发上限受单连接吞吐限制，但线程安全是绝对的。
- **WAL 模式**：其他进程 / 连接的读不会被本进程的写阻塞，读能看到已提交的写；
  单进程内因为全局 `RLock`，读写本来就是串行执行的（线程安全优先），
  WAL 的“读写并发”优势主要体现在多进程部署。
- **隔离级别**：WAL 下读事务看到的是**开始时点的快照**（快照隔离），
  不会被写事务阻塞，也读不到未提交的脏数据。
- **`busy_timeout=5000`**：遇到锁竞争时（WAL 模式下读操作不获取排他锁，读写互不阻塞，
  锁竞争只发生在多个写者之间）最多等 5 秒而不是立刻报错；超过 5 秒仍拿不到锁，
  SQLite 会抛 `database is locked`，业务层需要捕获并重试 / 降级。
- **`synchronous=NORMAL`**：WAL 模式下 NORMAL 只在 **checkpoint** 时做一次
  fsync，事务提交时不强制落盘——极端断电**可能丢失最近一次 checkpoint 之后
  已提交的事务**（通常只有几秒到几十秒的数据），不保证持久性；但 SQLite
  官方保证数据库文件**一致性**（绝不会因断电损坏）。要求每笔提交都落盘请用
  FULL（写入明显变慢）。（对单机 Agent 场景，这种极小概率的未落盘丢失通常
  业务可接受，且绝不会导致库文件损坏。）
- **`temp_store=MEMORY`**：排序等临时数据放内存。
- **原子 seq**：并发 / 多进程写不会分配重复序号。
- **WAL 自动 checkpoint**：SQLite 默认累计约 1000 页就自动合并回主库，批量导入结束还会显式 `wal_checkpoint(TRUNCATE)`，日常不用管；备份前手动跑一次更稳妥。

> 说明：SQLite 是单写者数据库。多个 Agent **同时写**同一文件时建议串行化或由 MCP 服务统一转发；读并发没有问题。

> 大白话：WAL 让读写不堵门，撞上写锁最多等 5 秒；`NORMAL` 写入最快，代价是断电时
> 最近刚写完的一批记忆可能丢（数据库不会坏）；一条都不能丢就改 `FULL`，写入会变慢；
> 临时数据放内存，排序不磨硬盘。

**MCP 服务端的写串行化**：HTTP 模式是 `ThreadingHTTPServer`，多个请求可以同时进来；
但所有请求共享同一个 `MemoryEngine` 实例，Backend 的每个方法都被进程内 `RLock`
串行化——写请求会自动排队，不会出现两个线程同时写 SQLite。多进程部署（如多个
`mnemosis-mcp` 进程写同一个库文件）是**反模式**：生产环境推荐“单进程 + 多线程”
或“每个进程独立库文件 + 应用层同步”，不要依赖 `busy_timeout` 硬抗长事务——
大导入期间其他进程的写请求超过 5 秒就会报 `database is locked`。

> 大白话：SQLite 一次只能容一个人写字。MCP 服务就像前台，把大家的写请求排成队，
> 一个一个递给 SQLite，免得大家挤在门口吵架（报锁超时）。

**注意长事务阻塞**：如果某个进程正在做 10 万条级大导入（长事务，可能持续几十秒），
其他进程此时发起写请求，等待超过 5 秒就会报 `database is locked`。
业务层必须捕获这个异常并重试 / 降级，或错峰执行大导入。
**Mnemosis 引擎层不会自动重试写锁冲突**——报错后直接中断，调用方需要自己实现
重试逻辑（如指数退避重试）。

> 大白话：大导入像一次大堵车，其他写请求等超过 5 秒就会直接报错，所以大活要错峰干。

**多进程读 + 单进程写**是官方支持的部署形态（WAL 下读不阻塞写）；**多进程同时写**
同一个文件才是反模式。

**多 Agent 共享记忆的架构建议**：让所有 Agent 统一调用同一个中心 MCP 服务进行读写，
或通过应用层消息队列异步同步；Mnemosis 引擎本身不支持多节点分布式写入。

**引擎生命周期管理**：程序退出前务必调用 `engine.close()`，或用
`with MemoryEngine(...) as engine:` 上下文管理；不关闭会泄漏文件句柄，
连接不断开时 `-wal` / `-shm` 也不会被正常清理。

**最低版本要求**：SQLite **≥ 3.35**（需要 upsert 和 `wal_checkpoint(TRUNCATE)`）。
大列表批量取用的 `json_each` 快路径需要较新的 SQLite（3.38+ 默认内置，更早版本
可能缺失），旧版本会在启动时自动探测并回退成 `VALUES` CTE——功能不受影响，
只是大批次会略慢。Python 3.9+ 自带 sqlite3 通常满足；
老系统（如 CentOS 自带旧 SQLite）建议使用较新的 Python 或升级系统 SQLite；
如果生产环境不允许升级，可安装 `pysqlite3-binary` 等预编译包并在代码里替换 `sqlite3` 模块。

**PRAGMA 的持久化级别**：`journal_mode=WAL` 会写进数据库文件头，是持久的；
`busy_timeout` / `synchronous` / `temp_store` / `cache_size` 都是**连接级别**的，
Mnemosis 每次打开连接都会自动设置。如果你直接用其他客户端连这个库，需要自己手动设置这些 PRAGMA。

**硬件与调优建议**：
- 强烈建议部署在 **SSD** 上；HDD 的随机读写会让 WAL 和冷启动性能明显变差；
- 库文件默认 `cache_size=-20000KB`（约 20MB 页缓存），批量导入会自动切到
  `-200000KB`（约 200MB）；500MB+ 的大库长期服务可自行调大
  `PRAGMA cache_size` 或开启 `PRAGMA mmap_size`，或在启动后调 `engine.warmup()`；
- 远程部署时库留在服务器本地磁盘，不要放 NFS / SMB 共享盘。

---

## 10. 数据生命周期：删除不是真的删

- `forget(id)` → `status` 改为 `recycled`，进回收站（还在库里，可恢复）。
- `restore(id)` → 恢复为 `active`。
- `purge()` → 真正 DELETE，不可恢复。
- `suppress_memories` → 检索时屏蔽，但不改状态。

查询都带 `status='active'` 过滤（下推到 SQL），回收站里的记忆不会出现在检索结果里。
`purge()` 硬删除时会在**同一事务**内级联删除该记忆的 `cues` / `links`（两端）/ `terms`，
不会留下孤儿索引行。级联清理由应用层手动保证，未使用 SQLite 原生外键约束
（避免外键检查带来的额外开销）。

> 大白话：普通删除是“先扔进回收站”，`purge` 才是“清空回收站”。这层保险是为误删设计的。

---

## 11. 备份、迁移与远程部署

**备份**：直接拷贝数据库文件即可。如果正在运行，最好连 `-wal`（预写日志）和
`-shm`（共享内存索引）一起拷，或先跑一次 `PRAGMA wal_checkpoint` 把 WAL 合并回主文件。
`-shm` 是 WAL 的辅助文件，正常退出会自动清理；异常退出残留可安全手动删除。
**`-wal` 里可能有未合并到主库的数据，绝不能手动删除**——保留它，SQLite 下次打开
会自动 recovery（合并并清理）。只拷贝主库而不拷 `-wal`，也会丢最近未 checkpoint 的写。
更稳的方式是用内置的 `export_memories` / `import_memories`（注意：导出的是
**活动记忆 + 意图/抑制清单**，不含回收站；同一份导出导入两次会产生重复记录）。

**安全拷贝前提**：拷贝前必须确保所有数据库连接已关闭，或至少执行
`PRAGMA wal_checkpoint(TRUNCATE)` 且没有活跃读事务；在有活跃连接时强行拷贝，
拷出来的库可能因 WAL 不一致而损坏。

**在线热备**：不想停服务时，用 SQLite 官方命令
`VACUUM INTO 'backup.db'` 生成一致性备份文件——它不锁库、不中断读写。

开了 embedding 的话，把 `memory.db.cache` / `memory.db.vec` 一起拷；只拷了主库也不要紧，用 `rebuild_missing_vectors()` 重建向量索引即可。

**远程部署**：Mnemosis 本质是“本地库 + 标准 MCP 服务”。

1. 把 `memory.db` 和 `mnemosis-mcp` 放到 VPS / NAS 上；
2. 通过 MCP（或你自定义的服务封装）对外提供接口；
3. 其他设备（电脑、手机上的 Agent 客户端）通过网络调用；
4. 不需要把数据库暴露成公网服务，也不需要额外部署 PostgreSQL / Redis。

> 大白话：数据库只在服务器本地，对外只开“记忆服务”这一个口子，安全性和部署难度都低。

---

## 12. 性能数字一览（10 万条级实测）

| 场景 | 官方/门禁基准 | 本次复核（2026-08-13） |
|---|---|---|
| 10 万条批量构建（`remember_many_chunked`） | 约 25~27 秒（旧路径 6~7 分钟） | —（沿用官方基准） |
| 构建期进程内存 / 库大小（10 万条） | 峰值 RSS 约 620MB / 库约 543MB | —（沿用官方基准） |
| 热查询 p99（200 次采样） | 约 1.8 ms | 约 1.8 ms（空载单线程） |
| 冷启动后的首次查询（10 万条） | 约 2.8 秒（之后回到毫秒级） | — |
| 零命中检索（热） | 约 1~2 ms（空载） | 约 7 ms（并发负载下） |
| 10 万条库高频词缓存命中 | 约 0.1~0.3 ms | avg 0.11~0.20 ms，max 0.61 ms |
| 高频泛查询（兜底缓存命中） | 约 0.1~0.6 ms | avg 0.11~0.20 ms |
| 10 万条 `sleep()` | 首次约 3.5 秒、稳态约 3.2 秒（曾 10.2 秒，耗时减少约 66%） | 3.46s / 稳态 3.15s |
| 睡眠后首次查询 p99（100 次） | 约 200 ms | 199.8 ms（p50 27ms / p95 60ms） |
| `get_many` 100 / 1000 / 5000 条 | 约 2.0 / 17.5 / 92 ms | 同左（本机中位数） |
| 4 线程并发写读冒烟 | 0 错误 | 0 错误（4×50 读写，200/200 落库） |

> 10 万条以上的扩展数据见 [扩展性基准](docs/scalability.md)：10 万 ~27s / 543MB / 620MB；
> 100 万 ~367s / ~5.1GB / ~2GB；热查询 p99 约 1.7ms。构建和冷启动强烈依赖磁盘性能，
> 换机器后数值会明显变化。

> **内存底线**：构建期峰值内存 100k 约 620MB、1M 约 2GB。小内存机器请预留余量——
> 100k 建议可用内存 ≥1GB，1M 建议 ≥2.5~3GB（或启用 Swap）；内存不足时构建可能直接崩溃。
> 内存吃紧时还可以降级：调小 `PRAGMA cache_size`（如 `-5000`）、减小
> `remember_many_chunked` 的分块大小、构建期临时关闭向量索引，都能明显压低峰值内存。

> 大白话：10 万条数据下，除了刚开机或刚睡醒的第一下要等一两秒，日常查询基本都在毫秒级，
> 人根本感觉不到延迟。要冲 100 万条的话，先确认机器内存够，别硬上。

> 数字来自本地 10 万条实库（`%TEMP%` 基准库）与 CI 性能门禁，机器不同会有波动；CI 每次跑 `ci_perf.py` 做“只拦灾难性退化”的宽松门禁，并积累趋势基线。

> 注：本次复核时另外两个基准在并发运行，命中查询一度升到 6.8~26 ms；进程内首次查询
> 944ms 属页缓存冷态。官方“绝对冷启动”首查约 2.8s；空载单线程下热查询回到 ~1.8ms。

---

## 13. 常见技术问题（FAQ）

**Q：能换 PostgreSQL 吗？**
存储层有 `Backend` 抽象，但目前只内置 `SQLiteBackend` 和 `DictBackend`（内存）。接 PostgreSQL 需要实现同一组接口；有真实需求可以按这个抽象扩展。

> 大白话：代码留了接口，但目前没写 PG 的驱动。想换得自己写个适配器。

**Q：`get_many` 为什么用 `CROSS JOIN json_each` 而不是 `WHERE id IN (...)`？**
长 `IN` 列表可能被 SQLite 优化器选成全表扫描（实测上百个 id 或特定复杂查询下就会退化）。`CROSS JOIN` 强制 json_each 当驱动表，memories 稳定走主键索引，且不依赖自动索引名（之前 `INDEXED BY` 的脆弱方案已替换）。
代码按**每批 1000 个 ID** 分块，JSON 序列化后约 40KB，远低于 SQLite 语句长度上限，
也不会有解析过慢的问题。

> 大白话：`IN` 列表太长时 SQLite 会“偷懒”去全表扫描，用 `json_each` 是逼着它老老实实走索引，保证速度不崩。

**Q：状态过滤为什么下推到 SQL？**
少传“已删除”的行；同时用 CI 计划巡检保证不会因此改走状态索引全扫。

> 大白话：让数据库在底层就把“已删除”的垃圾过滤掉，别传到内存里再筛，省内存又快。

**Q：缓存会不会返回旧数据？**
不会。兜底缓存命中后按 ID 重新查库取最新内容；所有写路径都会清缓存；嵌入向量缓存按内容哈希缓存，内容变了哈希就变。

> 大白话：不会。只要数据一改，缓存立刻清空；就算命中缓存，也会去库里拿最新内容，绝不给你看“过期记忆”。

**Q：向量索引是哪几个文件？**
主库之外会出现 `memory.db.cache`（文本 → 向量缓存）和 `memory.db.vec`（LSH 向量索引）。两者都可再生：删掉 `.vec` 后跑 `rebuild_missing_vectors()` 即可重建；不删也不影响关键词检索。

> 大白话：主库旁边多出的 `.cache` 和 `.vec` 是“语义加速器”，删了顶多变慢，记忆本身不丢。

**Q：LSH 向量索引会损失多少精度？**
`.vec` 是近似检索：向量先被哈希进多个桶（默认 8 个旋转桶）粗筛，再对桶内候选做**精确余弦重排**，所以最终排序仍是精确分数。10k 条以内因为数据量小、全量精确比对性能足够且能保证 100% 召回，所以直接全量精确比对；超过 10k 才走桶检索；候选不足时还会自动回退全扫描。参数（`bits` / `buckets_per_item` / `probe` / `max_candidates`）都可在构造 `VectorIndex` 时调整；默认参数是“够用优先”，追求更高召回时按业务数据调优。
参数都可在构造 `VectorIndex` 时调整；默认参数是“够用优先”，追求更高召回时按业务数据调优。

> 大白话：粗筛时靠“猜”（近似），但最终排序靠“精算”（精确）。给你的分数是精确的，只是不用把全库都比一遍，所以快。

**关于召回率的诚实说明**：本项目对默认参数做过随机向量压测——10k 条以内因为走
全量精确比对，召回是 100%；10 万级随机向量上**纯桶候选**的 Top-10 命中率很低
（实测 <5%），真实语义向量因有聚类结构通常好一些，但 LSH 桶召回不应被当成
精确检索。当前实现有兜底：候选过少会自动回退全量精确扫描，所以小库始终精确；
需要更高召回时请调大 `probe` / `buckets_per_item`，或按业务数据 A/B 验证。

**Q：断电会不会把数据库弄坏？**
WAL + `synchronous=NORMAL` 已把 SQLite **逻辑层**的损坏概率压到极低，但极端断电或文件系统崩溃仍可能导致文件打不开。遇到这种情况先跑 `PRAGMA integrity_check` 检查；SQLite 自带 `.recover` 命令可以把能抢救的数据导出为 SQL，建议定期备份主库（或直接拷文件）作为最可靠的兜底。

> 大白话：WAL 已经帮你扛住了绝大多数断电场景；真碰上打不开，先体检（integrity_check），不行就用 `.recover` 抢救，最稳的还是平时备份。

**Q：`-wal` 文件会不会无限变大？**
不会。SQLite 默认自动 checkpoint（累计约 1000 页触发一次），批量导入结束也会显式合并；只有长时间高频写入又不 checkpoint 才会明显膨胀，备份前手动跑一次 `PRAGMA wal_checkpoint(TRUNCATE)` 即可。

注意：如果**长事务卡住**（如大导入中途未提交），或存在**未释放的活跃读事务**
（即使只读也会阻塞 checkpoint），自动 checkpoint 都会被阻塞，WAL 会持续膨胀。
根因是 checkpoint 必须等所有活跃读事务结束才能截断 WAL，所以代码层要确保
读事务及时关闭 / 释放（避免长连接一直挂着只读事务），并定期看一眼 `memory.db-wal`
的文件大小，超过主库的一定比例就手动跑一次 `PRAGMA wal_checkpoint(TRUNCATE)`。

> 大白话：WAL 就像“待办草稿纸”，写满一页会自动誊抄到正本里；但如果有人一直占着
> 草稿纸不松手（卡住的长事务），草稿纸就会越堆越厚。定期看下文件大小，或手动跑
> 一次合并命令就能解决。

**Q：能把库放在 NAS / 网盘上直接共享吗？**
不要直接共享主库文件。SQLite 官方不建议把库放在 NFS / SMB 等网络文件系统上（文件锁不可靠，可能损坏数据库）。正确姿势是库留在服务器本地，通过 MCP 服务给其他设备调用。

> 大白话：库文件别放到网盘上“多人共用”，SQLite 的文件锁在网络盘上不靠谱。正确做法是库留在本机，别人通过接口来访问。

**Q：分词器能换成 jieba 之类吗？**
目前 `tokenize()` 是内置实现（中文分词、停用词过滤、日期归一、同义词扩展），
不是可插拔接口；要换分词器需要二次开发（替换 `mnemosis.types.tokenize`）。
10 万级实测内置分词足够快，暂不计划默认引入第三方分词依赖。

**Q：settings 里的参数改了立刻生效吗？**
引擎启动时从 `settings` 表加载一次（如 `decay_rate`）。用 `calibrate_decay_rate`
工具修改会**立即更新内存中的遗忘曲线并持久化**；直接手动改数据库的 `settings`
表则需要重启引擎（或重新构造引擎）才生效。

**Q：100 万条还能用吗？**
实测能存也能查：100 万条构建约 6 分钟、库约 5.1GB，热查询 p99 约 1.7ms。但构建期内存与库体积明显上升，官方推荐线仍是 10 万级日常使用；百万级建议配合向量索引做候选剪枝。

**Q：数据库文件很大怎么办？**
可以 `purge()` 清回收站，跑 `sleep()` 让系统把低价值记忆自动回收，也可以用 `VACUUM` 压缩文件空洞（Mnemosis 不自动执行，避免阻塞）。

注意：`VACUUM` 慢不是因为 WAL，而是它要**重写整个数据库文件**（全表扫描 + 重建，
执行完会清空 WAL）。大库建议先执行 `PRAGMA wal_checkpoint(TRUNCATE)` 把 WAL
合并回主文件，再挑业务低峰期压缩。`VACUUM` 还需要**约等于原库大小的额外磁盘空间**，
磁盘余量不足时可能失败，先确认空间再执行。执行期间会持有排他锁，**阻塞所有读写**，
必须选在没有请求的低峰期跑，否则其他请求会报 `database is locked`。

> 大白话：压缩前得先把“待办日志”（WAL）全合并到主文件里，不然 SQLite 会罢工。

---

## 14. 相关文档

- [奶龙级入门教程——基本功能介绍版](奶龙级入门教程——基本功能介绍版.md)：零基础功能总览
- [架构与模块](docs/architecture.md)：模块职责与数据流
- [记忆模型](docs/memory-model.md)：人脑机制 ↔ 代码实现对照
- [MCP 接入](docs/mcp-quickstart.md)：各客户端配置
