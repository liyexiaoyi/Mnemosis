# Mnemosis 存储功能介绍（技术人员版）

> 这份文档写给想接入 Mnemosis 的开发者、部署者和二次开发者。
> 默认你懂 SQL / 数据库的基本概念，但每个技术结论都会补一句大白话“为什么这么做”。
> 目标是：看完能回答“数据放哪、怎么存取、快不快、稳不稳、能不能备份迁移”。

---

## 1. 一句话总结

Mnemosis 的存储层 = **一个 SQLite 单文件数据库 + 三层内存缓存 + 一批为了“快”而做的 SQL / 索引优化**。

默认零外部依赖：不需要 PostgreSQL、不需要 Redis、不需要向量数据库。
整个记忆库就是一个 `.db` 文件，拷走它，记忆就带走了。

> 大白话：这不是“为了炫技”的架构，而是单机 / 单 Agent 场景下最省心、最不容易坏的方案。

---

## 2. 为什么选 SQLite

| 优点 | 说明 |
|---|---|
| 单文件、零运维 | 一个 Agent 或一台 NAS 就是典型场景，不需要装数据库服务 |
| 事务与 ACID | 写入要么全部成功、要么全部回滚，记忆不会写到一半坏掉 |
| 性能足够 | 10 万条级记忆库，热查询约 1~2ms；高频缓存命中约 0.1~0.3ms |
| 可移植 | 文件即备份；也支持 `":memory:"` 纯内存模式（测试 / 临时用） |
| 远程部署简单 | 库文件放 VPS / NAS，通过 MCP 服务对外提供，不必把数据库暴露成公网服务 |

> 大白话：单机场景给记忆单独装一个数据库服务属于“杀鸡用牛刀”。SQLite 正好卡在“够快、够稳、不用装”的点上。

什么时候不适合：**多进程同时高频写同一个文件**。SQLite 是单写者数据库，多个 Agent 同时写建议串行化或由 MCP 服务统一转发；读并发没有问题。

> 警告：不要把 `memory.db` 直接放在 NFS / SMB 这类网络共享盘上让多台机器共用——SQLite 的文件锁在网络文件系统上不可靠，可能导致数据库损坏。远程部署的正确姿势是：库留在服务器本地，对外只开 MCP 服务接口，而不是共享数据库文件本身。

---

## 3. 数据模型：5 张表

全部是普通 SQLite 表：

### `memories` —— 记忆主表

| 字段 | 作用 |
|---|---|
| `id` (TEXT PRIMARY KEY) | 记忆唯一 ID（UUID） |
| `kind` | `episodic`（情景）/ `semantic`（语义） |
| `content` / `content_hash` | 内容原文 + 哈希（语义去重用） |
| `source_json` / `cues_json` | 来源信息与线索的 JSON 快照 |
| `created_at` / `updated_at` / `last_access_at` | 时间戳 |
| `importance` / `strength` / `confidence` | 重要度 / 强度 / 置信度 |
| `storage_strength` | 长期存储强度（涨得慢、掉得慢） |
| `retrieval_successes` / `retrieval_failures` | 回忆成功 / 失败次数 |
| `review_streak` / `last_review_at` | 复习状态 |
| `revision_count` | 被修改过几次（再巩固） |
| `status` | `active` / `recycled`（回收站） |
| `seq` | 全局自增序号（按时间排序用） |
| `context` / `affect` / `evidence_count` | 情境 / 情绪 / 证据数 |

### `links` —— 关联图

`(src, dst, weight)`，主键 `(src, dst)`。
记录“记忆 A 和记忆 B 有关联，权重多少”，用于关联激活、模式补全等。

### `cues` —— 线索索引

`(cue, memory_id)`，主键 `(cue, memory_id)`。
线索就是“想起这条记忆的钥匙”，一张记忆可以有多把钥匙。

### `terms` —— 分词索引

`(term, memory_id, kind)`，主键 `(term, memory_id)`。
记忆内容分词后的词条，检索时用来快速找候选。

### `settings` —— 键值设置

存 `decay_rate`（遗忘曲线参数）等运行期配置，重启后自动恢复。

> 大白话：`memories` 是“卡片本身”，`links` 是“卡片之间的线”，`cues` / `terms` 是“查卡片的目录”，`settings` 是“设置页”。

---

## 4. 索引设计：为什么快

| 索引 | 服务谁 | 说明 |
|---|---|---|
| `sqlite_autoindex_memories_1`（id 主键） | 按 ID 批量取记忆 | TEXT 主键自动索引 |
| `idx_semantic_hash`（部分索引，仅 semantic） | 语义去重 | `(kind, content_hash)` 唯一 |
| `idx_memories_status_seq` | 最近记忆 | `(status, seq)` |
| `idx_memories_status_importance` | 最强记忆 | `(status, importance)` |
| `idx_memories_status_kind` | 按类型过滤 | `(status, kind)` |
| `cues` / `terms` 主键 | 线索 / 词条反查 | 覆盖索引，查词条不用回表 |
| `idx_links_dst` | 反向关联 | 图遍历用 |

**重点：CI 里有 SQL 执行计划巡检（`audit_query_plans`）**，每次提交都检查核心查询是否仍走预期索引。
历史上修过一个真实事故：`get_many` 因带 `status` 过滤被 SQLite 优化器改成状态索引全扫，50 次查询从 1ms 变成 1434ms——现在这类退化会被 CI 直接拦下。

> 大白话：索引像书的目录。慢往往不是数据多，而是 SQLite 没走目录、把整本书翻了一遍。CI 就是盯着它“必须翻目录”。“覆盖索引”更省：目录页里直接写了答案，连翻到正文（回表）那一步都省了。

---

## 5. 写路径：一条记忆是怎么落库的

`remember(...)` 大致做四件事：

1. **打分与建模**：算重要度、贴时间戳、建 `MemoryItem`。
2. **写入主表**：
   - 情景记忆：直接 INSERT；
   - 语义记忆：按 `(kind, content_hash)` 去重，存在就合并证据数。
3. **写目录**：`cues`、`terms` 批量写入。
4. **建关联**：和“共享线索 / 同现”的记忆建 `links`（带权重）。

### 批量写入：`remember_many` / `remember_many_chunked`

`remember_many` 把上面这些全部批量化：

- 一个事务内 `executemany` 插入；
- `seq` 用 `INSERT ... SELECT MAX(seq)+1` 原子分配（多进程也不会重号）；
- 关联边数按规模自动收缩（起步 32 条 / 记忆，10 万条时约 10 条，100 万条时至少 8 条），避免边表把库写爆。注意：关联边是**双向落库**的（A→B 和 B→A 各存一行），所以实测行数约为“预算 × 记忆数 × 2”（10 万条：预算 10，实测约 196 万条边）。

`remember_many_chunked` 是 10 万条级导入专用路径：分块写入 + 增量建链 + 批量导入模式。

> 注意：`remember_many_chunked` **不是原子操作**——它按块提交，如果中途失败，前面已导入的块会留在库里。大导入建议用可重试的流程（先清空目标再重导）。

> 实测（10 万条）：
> - `remember_many_chunked` 约 **25 秒**（旧路径约 6~7 分钟，最初版本要 2 小时以上）；
> - 构建期进程常驻内存（RSS）约 **440MB**，数据库文件约 **520MB**；100 万条约 6 分钟 / 4.9GB。

---

## 6. 读路径：一次检索发生了什么

`recall(query)` 的链路：

```text
分词 / 同义词扩展
  → 查词频（df，决定哪些词值得当候选）
  → 取候选 ID（走 terms/cues 索引）
  → 打分（关键词重叠 + 重要度 + 遗忘曲线 + 情境/情绪/置信度…）
  → 可选：向量重排（只对 top-64 候选）
  → 图后处理（关联激活 / 模式补全 / 模式分离 / 竞争抑制）
  → 强化命中项、记录失败、安排复习
```

几个“为了快”的关键实现：

- **高频词跳过**：单词语频（df）太大时不再物化全部候选 ID（候选上限 5000），直接进兜底池，避免几万条候选。
- **零命中兜底**：无词法信号时用“最近 150 + 最强 50”双池，走 `(status, seq)` / `(status, importance)` 索引，查询只要几毫秒。
- **批量取 ID**：`get_many` 小列表用 `VALUES` CTE，大列表（≥64）用 `CROSS JOIN json_each`，状态过滤下推到 SQL——保证任何规模都走主键索引。
- **后处理限流**：泛查询跳过竞争抑制 / 激活扩展，单次召回最多抑制 12 条。

> 大白话：检索不是“把整库读出来再比”，而是“先用目录圈出一小撮候选，再对候选精算分数”。

---

## 7. 内存缓存：三层加速器 + 睡眠后预热

| 缓存 | 存什么 | 上限 / 策略 | 失效 |
|---|---|---|---|
| 词频 / 倒排缓存 | 词条 → ID、词频 | 50 万条上限（FIFO） | 写操作清空 |
| 嵌入向量缓存 | 文本 → 向量 | 默认约 512MB（按真实内存估算），FIFO | 写操作清空 + 字节淘汰 |
| 兜底结果缓存 | 泛查询结果（id + 分数 + 原因） | 默认 32 条、TTL 15 秒、驱逐率高时自动扩容到 256 | remember / update / sleep / review 等写路径清空 |

缓存命中后仍会**重新取最新记忆内容**再返回，不会把旧文本喂给 Agent；写入即失效，缓存不会让你读到“刚删掉”的记忆。

### 睡眠后预热（post-sleep warmup）

`sleep()` 会做大量数据库整理，可能把操作系统页缓存和 SQLite 缓冲“挤出去”，导致睡醒后第一次查询变慢。所以睡眠结束后会启动一个后台线程，按以下规则挑出最多 **8 个线索（cue）** 预先查一遍，把缓存和页缓存“暖起来”：

```
预热得分 = (0.6 × 重要度 + 0.4 × (1 - 可提取度)) + 0.2 × 热度占比
没有热度信号时：= 0.6 × 重要度 + 0.4 × (1 - 可提取度)  ← 与旧版完全一致
```

- 重要度高的记忆优先；
- 快要遗忘的（可提取度低）优先，相当于“趁还记得先巩固”；
- 最近常被问到的线索优先（热度维度，来自最近 100 条查询日志）。
热度作为“加分项”，没有热度的记忆排序与旧版完全一致；只有最近被问过的线索会额外加分。

`stats()["warmup"]` 会暴露 `cues` / `recalls` / `hits` / `hit_rate`，可以观察预热命中率：

```python
engine.sleep()
print(engine.stats()["warmup"])
# {'cues': 8, 'recalls': 6, 'hits': 4, 'hit_rate': 0.6667}
```

`is_active` 表示“预热观测窗口（睡眠后 5 分钟内）是否仍有效”。窗口过期后，
`recalls` / `hits` / `hit_rate` 保留的是上一轮的最终数值，配合 `is_active=False`
一起看，避免把历史数据当成实时数据。

> 大白话：睡眠像“整理房间”，整理完桌子会乱；预热就是“提前把常用东西摆回手边”，让用户醒来第一句就有响应速度。

---

## 8. 可选：向量副文件（不开 embedding 就没有）

默认只用一个 `memory.db`。开启 embedding（Ollama 或 OpenAI 兼容接口）后，主库旁边会多出两个 SQLite 副文件：

| 文件 | 存什么 | 丢了会怎样 |
|---|---|---|
| `memory.db.cache` | 文本哈希 → 向量 的持久缓存（避免反复调 embedding API） | 只是变慢：重新调 API 计算即可，记忆无损 |
| `memory.db.vec` | LSH 向量索引（向量 + 桶），供语义重排 / 语义召回 | 语义检索失效；跑 `rebuild_missing_vectors()`（或 MCP 工具 `rebuild_vectors`）可重建 |

- 副文件都是可再生的：删掉它们，关键词 / n-gram 检索完全不受影响；
- 备份时最好把主库和两个副文件一起拷走；如果只拷主库，记得先跑一次重建向量；
- `.cache` 只存 SHA-256 哈希和向量，不存原文；`.vec` 按记忆 id 关联，不存内容。
- 主库记忆被删除 / 修改时，两个副文件**不会逐条同步清理**：回收站记忆靠检索时的 `status` 过滤兜底，不会出现在结果里；内容被 `update` 改过之后，旧向量会留在 `.vec`（语义候选分可能基于旧向量，但返回的文本始终是库里最新内容）。想彻底刷新：删掉 `memory.db.vec` 后跑 `rebuild_missing_vectors()` 全量重建，有 `.cache` 就不用重新调 embedding API。

> 大白话：主库是“记忆本身”，两个副文件只是“语义检索的加速器”。加速器坏了可以重建，记忆本身不会丢。

---

## 9. 并发与可靠性

- **线程安全**：后端所有方法用 `RLock` 包裹，4 线程并发写读冒烟 0 错误。
- **WAL 模式**：读写不互相阻塞，读能看到已提交的写。
- **`busy_timeout=5000`**：写锁竞争时最多等 5 秒而不是立刻报错。
- **`synchronous=NORMAL`**：WAL 下兼顾性能与崩溃安全。
- **`temp_store=MEMORY`**：排序等临时数据放内存。
- **原子 seq**：并发 / 多进程写不会分配重复序号。
- **WAL 自动 checkpoint**：SQLite 默认累计约 1000 页就自动合并回主库，批量导入结束还会显式 `wal_checkpoint(TRUNCATE)`，日常不用管；备份前手动跑一次更稳妥。

> 说明：SQLite 是单写者数据库。多个 Agent **同时写**同一文件时建议串行化或由 MCP 服务统一转发；读并发没有问题。

> 大白话：WAL 让“读”和“写”不互相堵门；偶尔撞上写锁最多等 5 秒而不是直接报错；NORMAL 在 WAL 下既保安全又不拖慢速度；临时数据放内存，排序不会去磨硬盘。

---

## 10. 数据生命周期：删除不是真的删

- `forget(id)` → `status` 改为 `recycled`，进回收站（还在库里，可恢复）。
- `restore(id)` → 恢复为 `active`。
- `purge()` → 真正 DELETE，不可恢复。
- `suppress_memories` → 检索时屏蔽，但不改状态。

查询都带 `status='active'` 过滤（下推到 SQL），回收站里的记忆不会出现在检索结果里。

> 大白话：普通删除是“先扔进回收站”，`purge` 才是“清空回收站”。这层保险是为误删设计的。

---

## 11. 备份、迁移与远程部署

**备份**：直接拷贝数据库文件即可。如果正在运行，最好连 `-wal` / `-shm` 一起拷，或先跑一次 `PRAGMA wal_checkpoint` 把 WAL 合并回主文件。更稳的方式是用内置的 `export_memories` / `import_memories`。

开了 embedding 的话，把 `memory.db.cache` / `memory.db.vec` 一起拷；只拷了主库也不要紧，用 `rebuild_missing_vectors()` 重建向量索引即可。

**远程部署**：Mnemosis 本质是“本地库 + 标准 MCP 服务”。

1. 把 `memory.db` 和 `mnemosis-mcp` 放到 VPS / NAS 上；
2. 通过 MCP（或你自定义的服务封装）对外提供接口；
3. 其他设备（电脑、手机上的 Agent 客户端）通过网络调用；
4. 不需要把数据库暴露成公网服务，也不需要额外部署 PostgreSQL / Redis。

> 大白话：数据库只在服务器本地，对外只开“记忆服务”这一个口子，安全性和部署难度都低。

---

## 12. 性能数字一览（10 万条级实测）

| 场景 | 数字 |
|---|---|
| 10 万条批量构建（`remember_many_chunked`） | 约 25 秒（旧路径 6~7 分钟） |
| 构建期进程内存 / 库大小（10 万条） | RSS 约 440MB / 库约 520MB |
| 真实命中检索（热查询，页缓存已就绪） | 约 1~2 ms |
| 冷启动后的首次查询（10 万条） | 约 2.5 秒（之后回到毫秒级） |
| 零命中检索（热） | 约 1~2 ms |
| 10 万条库高频词缓存命中 | 约 0.1~0.3 ms |
| 高频泛查询（兜底缓存命中） | 约 1.5~2 ms |
| 10 万条 `sleep()` | 约 2.4 秒（曾 10.2 秒，多轮优化后 -76%） |
| 睡眠后首次查询 p99（100 次） | 约 215 ms |
| `get_many` 100 / 500 / 2000 条 | 约 1 / 5 / 22 ms（本机中位数，随机器波动） |
| 4 线程并发写读冒烟 | 0 错误 |

> 数字来自本地 10 万条实库（`%TEMP%` 基准库）与 CI 性能门禁，机器不同会有波动；CI 每次跑 `ci_perf.py` 做“只拦灾难性退化”的宽松门禁，并积累趋势基线。

---

## 13. 常见技术问题（FAQ）

**Q：能换 PostgreSQL 吗？**
存储层有 `Backend` 抽象，但目前只内置 `SQLiteBackend` 和 `DictBackend`（内存）。接 PostgreSQL 需要实现同一组接口；有真实需求可以按这个抽象扩展。

**Q：`get_many` 为什么用 `CROSS JOIN json_each` 而不是 `WHERE id IN (...)`？**
长 `IN` 列表会被 SQLite 优化器选成全表扫描（实测 50 个 id 就退化）。`CROSS JOIN` 强制 json_each 当驱动表，memories 稳定走主键索引，且不依赖自动索引名（之前 `INDEXED BY` 的脆弱方案已替换）。

**Q：状态过滤为什么下推到 SQL？**
少传“已删除”的行；同时用 CI 计划巡检保证不会因此改走状态索引全扫。

**Q：缓存会不会返回旧数据？**
不会。兜底缓存命中后按 ID 重新查库取最新内容；所有写路径都会清缓存；嵌入向量缓存按内容哈希缓存，内容变了哈希就变。

**Q：向量索引是哪几个文件？**
主库之外会出现 `memory.db.cache`（文本 → 向量缓存）和 `memory.db.vec`（LSH 向量索引）。两者都可再生：删掉 `.vec` 后跑 `rebuild_missing_vectors()` 即可重建；不删也不影响关键词检索。

**Q：`-wal` 文件会不会无限变大？**
不会。SQLite 默认自动 checkpoint（累计约 1000 页触发一次），批量导入结束也会显式合并；只有长时间高频写入又不 checkpoint 才会明显膨胀，备份前手动跑一次 `PRAGMA wal_checkpoint(TRUNCATE)` 即可。

**Q：能把库放在 NAS / 网盘上直接共享吗？**
不要直接共享主库文件。SQLite 官方不建议把库放在 NFS / SMB 等网络文件系统上（文件锁不可靠，可能损坏数据库）。正确姿势是库留在服务器本地，通过 MCP 服务给其他设备调用。

**Q：100 万条还能用吗？**
实测能存也能查：100 万条构建约 6 分钟、库约 4.9GB，热查询约 1.3ms。但构建期内存与库体积明显上升，官方推荐线仍是 10 万级日常使用；百万级建议配合向量索引做候选剪枝。

**Q：数据库文件很大怎么办？**
可以 `purge()` 清回收站，跑 `sleep()` 让系统把低价值记忆自动回收，也可以用 `VACUUM` 压缩文件空洞（Mnemosis 不自动执行，避免阻塞）。

---

## 14. 相关文档

- [奶龙级入门教程——基本功能介绍版](奶龙级入门教程——基本功能介绍版.md)：零基础功能总览
- [架构与模块](docs/architecture.md)：模块职责与数据流
- [记忆模型](docs/memory-model.md)：人脑机制 ↔ 代码实现对照
- [MCP 接入](docs/mcp-quickstart.md)：各客户端配置
